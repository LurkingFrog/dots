{
  // Place your snippets for javascriptreact here. Each snippet is defined under a snippet name and has a prefix, body and
  // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
  // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the
  // same ids are connected.
  // Example:
  // "Print to console": {
  // 	"prefix": "log",
  // 	"body": [
  // 		"console.log('$1');",
  // 		"$2"
  // 	],
  // 	"description": "Log output to console"
  // }
  "Flow Doc": {
    "prefix": "flo",
    "body": ["// @flow"],
    "description": "Add a flow comment to a js file"
  },
  "Flow Type": {
    "prefix": "fty",
    "body": ["export type $1 = {|$2|};"],
    "description": "Add a new data type for flow"
  },
  "React Component class": {
    "prefix": "rcls",
    "body": [
      "/* $1 */",
      "type $1Props = {|",
      "  className?: string",
      "|};",
      "",
      "class Unstyled$1 extends React.Component<$1Props, {}> {",
      "  render() {",
      "    return (",
      "      <div className={this.props.className} name=\"$1\">",
      "",
      "      </div>",
      "    );",
      "  }",
      "}",
      "",
      "let Connected$1 = connect(null, null)(Unstyled$1);",
      "",
      "const $1 = styled(Connected$1)`",
      "",
      "`;",
      ""
    ],
    "description": "Create a new react component stub"
  }
}
